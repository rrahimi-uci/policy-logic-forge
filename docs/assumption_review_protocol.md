# Assumption review protocol

Every assumption has a typed formula and at least one source SHA-256-bound
provenance span. The solver result for each implication is reported separately
from human source support and acceptability. Sets are checked jointly for
consistency, vacuity, and minimally sufficient subsets. Unknown or timeout is
never promoted to validity, and legal admissibility remains blinded human
judgment.
