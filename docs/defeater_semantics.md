# Exception-reading selection

J1B preregisters three frozen alternatives: `defeater_or`, `conjunctive`, and
explicit `refuse`. Selection uses source annotation agreement only. Instrument
outcomes (`OE`, `AFS`, or downstream quality) are prohibited from the
selection dataset. Until an approved annotation comparison is supplied, the
retained result remains `unrun` and the IR default is `unset`.
