# G3 disagreement and invalidation taxonomy

Reports distinguish `valid`, `underpowered`, and `invalid`. Invalid causes
include accessible leakage canaries, predictive label permutations, malformed
or incomplete controls, missing model clusters, and statistics that cannot be
computed. A disagreement between a source signal and OE is an observation; it
is not silently relabelled as extraction failure or omitted.
