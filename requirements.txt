openai>=1.59.7
python-dotenv>=1.0.0
PyPDF2>=3.0.1
langchain-text-splitters>=0.2.0
python-docx>=1.1.0

# compiler/ (DMN/BPMN emission) — see k-to-code.md. Declared explicitly rather
# than relying on a transitive install (flagged in k-to-code.md's risk table).
lxml>=5.0.0
