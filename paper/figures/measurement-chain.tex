\begin{tikzpicture}[
  node distance=3.5mm and 4.5mm,
  stage/.style={draw=evidencegray!45, rounded corners=2pt, fill=white,
    minimum height=9mm, text width=.205\linewidth, align=center,
    font=\scriptsize},
  plane/.style={rounded corners=2pt, minimum height=8mm,
    text width=.255\linewidth, align=center, font=\scriptsize},
  flow/.style={-{Latex[length=2mm]}, semithick, draw=evidencegray!75},
  gate/.style={-{Latex[length=2mm]}, semithick, dashed, draw=evidencegray!65}
]
  \node[stage] (source) {\textbf{Source}\\ source document + addressable spans};
  \node[stage, right=of source] (rule) {\textbf{Candidate}\\ rule + evidence packet};
  \node[stage, right=of rule] (compiler) {\textbf{Bounded compiler}\\ typed IR + proof obligations};
  \node[stage, right=of compiler] (artifact) {\textbf{Artifact}\\ FEEL/DMN + execution trace};

  \draw[flow] (source) -- (rule);
  \draw[flow] (rule) -- (compiler);
  \draw[flow] (compiler) -- (artifact);

  \node[plane, below=8mm of rule, xshift=-18mm,
    fill=evidenceblue!10, draw=evidenceblue!45] (afs)
    {\textcolor{evidenceblue}{\textbf{AFS}}\\independent source annotation};
  \node[plane, right=5mm of afs,
    fill=evidenceamber!10, draw=evidenceamber!50] (ops)
    {\textcolor{evidenceamber}{\textbf{OPS}}\\manifest, coverage, refusal};
  \node[plane, right=5mm of ops,
    fill=evidenceteal!10, draw=evidenceteal!45] (oe)
    {\textcolor{evidenceteal}{\textbf{OE}}\\gold + independent execution};

  \draw[gate] (source.south) |- (afs.north);
  \draw[gate] (rule.south) -- (afs.north);
  \draw[gate] (compiler.south) -- (ops.north);
  \draw[gate] (artifact.south) |- (oe.north);

  \node[rounded corners=2pt, fill=evidencegray!10, draw=evidencegray!45,
    below=5mm of ops, minimum height=8mm, text width=.63\linewidth,
    align=center, font=\scriptsize] (ledger)
    {\textbf{Evidence ledger}: state + denominator + hashes + exclusions
    $\Longrightarrow$ permitted claim};
  \draw[flow] (afs) -- (ledger);
  \draw[flow] (ops) -- (ledger);
  \draw[flow] (oe) -- (ledger);

  \node[rounded corners=2pt, fill=evidencered!9, draw=evidencered!50,
    below=3mm of compiler, xshift=23mm, text width=.16\linewidth,
    align=center, font=\scriptsize] (refusal) {visible refusal};
  \draw[-{Latex[length=1.8mm]}, draw=evidencered!80, semithick]
    (compiler.south east) to[out=-25,in=110] (refusal.north);
\end{tikzpicture}
