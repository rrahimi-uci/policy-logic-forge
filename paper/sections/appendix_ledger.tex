\section{Evidence ledger}

\input{tables/evidence-ledger}

The ledger is intentionally conservative. A row can move from \emph{unrun} to
\emph{measured} only when its manifest, denominator, protocol, and retained raw
observations are present. A changed prompt, model, corpus, or schema creates a
new row rather than overwriting an earlier observation.
